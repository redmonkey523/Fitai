/**
 * Global TypeScript declarations for React Native
 */

// React Native global
declare const __DEV__: boolean;

// Extend the global Window interface if needed
declare global {
  interface Window {
    // Add any custom window properties here
  }
}

export {};

